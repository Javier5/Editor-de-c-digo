impl eframe::App for EditorApp { fn update(&mut self, ctx: &egui::Context, _frame: &mut eframe::Frame) { egui::CentralPanel::default().show(ctx, |ui| { ui.heading("Editor de Código"); // Ajuste para ocupar el espacio disponible sin ser tapado por el teclado let available_height = ui.available_height(); let text_edit = egui::TextEdit::multiline(&mut self.api.buffers.get_mut(&self.api.active_buffer).unwrap().lines[0]) .desired_width(f32::INFINITY) .desired_rows(20); ui.add_sized(ui.available_size(), text_edit); }); } fn on_exit(&mut self, _gl: Option<&eframe::glow::Context>) { println!("Guardando..."); } }
