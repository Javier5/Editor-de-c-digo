impl eframe::App for EditorApp { fn update(&mut self, ctx: &egui::Context, _frame: &mut eframe::Frame) { egui::CentralPanel::default().show(ctx, |ui| { ui.heading("Editor de Código"); let buffer = self.api.buffers.get_mut(&self.api.active_buffer).unwrap(); if ui.add(egui::TextEdit::multiline(&mut buffer.lines[0])).changed() { self.api.notify(crate::core::events::EditorEvent::TextChanged); } }); } fn on_exit(&mut self, _gl: Option<&eframe::glow::Context>) { println!("Guardando cambios antes de cerrar..."); // Aquí iría tu lógica de guardado a disco } }
